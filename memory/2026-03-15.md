# 2026-03-15 — Tageslog

## Lern-Bot — Feature-Update v0.5.0

### Was gebaut wurde
Alle 5 Features aus der Speechify-Recherche implementiert und deployt:

1. **Sentence Highlighting + Auto-Scroll** — `boundary`-Event aus Web Speech API → Satz wird gold hinterlegt, Transcript scrollt automatisch mit
2. **Speed Slider** — stufenlos 0.5× bis 2.5×, persistiert in `lernbot_speed` (localStorage)
3. **Bionic Reading Toggle** — im Header, erste 40% jedes Wortes fett, persistiert in `lernbot_bionic`
4. **Lesefortschritt + Wiedereinstieg** — `lernbot_progress_${docId}` speichert letztes Kapitel, Resume-Banner beim nächsten Öffnen
5. **OpenAI TTS Route** — `/api/tts` — mit Key: OpenAI `tts-1` + voice `nova`; ohne Key: graceful fallback auf Web Speech

### Commits
- `8c19453` — v0.4.0 (Versionsbump, unpdf)
- `2233d55` — v0.5.0 — alle 5 Features, Build grün

### Status
- Live: <https://lern-bot.vercel.app>
- Vercel auto-deployed via GitHub (`Vic3d/Lern-Bot`, branch `main`)
- Build clean, alle Routen grün

### Technische Details
- Write-Tool konnte vorhandene .tsx-Dateien nicht überschreiben → Workaround via Python `open(..., 'w')`
- Transcript.tsx nutzt `useMemo` + `useRef` für sentence-Refs, kein dangerouslySetInnerHTML
- AudioPlayer-Props: `onBoundary`, `onEnded`, `speed`, `onSpeedChange` (von Reader-Page verwaltet)

## Projekt-Dateipfade (aktuell)
- Workspace: `/data/.openclaw/workspace/lernbot/`
- Upload-Route: `app/api/documents/upload/route.ts`
- AudioPlayer: `app/components/AudioPlayer.tsx`
- Transcript: `app/components/Transcript.tsx`
- Reader: `app/reader/[id]/page.tsx`
- SSH-Key Vic3d: `/data/.ssh/id_ed25519_vic3d`

---

## Lern-Bot — Bugfixes (08:00–08:26 UTC)

### Fix 1: PDF.js Worker Version Mismatch
- `pdfjs-dist@5.5.207` war per npm installiert, aber CDN-Worker war `3.11.174`
- Fix: npm-Import direkt (`import('pdfjs-dist')`), Worker-URL von jsdelivr dynamisch
- Commit: `4d3b988` → Redeploy-Trigger: `07dea09`

### Fix 2: TTS Auto-Advance Bug
- `speechSynthesis.cancel()` beim Seek triggerte `utterance.onend` → `onEnded?.()` → Kapitel wechselte automatisch
- Alter Ansatz: `suppressEndRef` mit 100ms `setTimeout` → Race Condition
- Neuer Fix: **Generation Counter** `ttsGenRef` — bei jedem `stopTTS()` inkrementiert, `onend`/`onboundary`/`onerror` prüfen ob `ttsGenRef.current === myGen`, sonst return
- Commit: `8896e90`

### Fix 3: Kapitel-Titel (echte Titel statt "Teil 1", "Teil 2")
- AKAD-PDFs haben Inhaltsverzeichnis auf Seite 2 mit echten Titeln + Seitenzahlen
- Neues `extract_pdf.py` erkennt TOC automatisch (≥4 Einträge, nicht-absteigende Seitenzahlen)
- TOC-Einträge: z.B. "1.1 Tragelemente ebener Systeme 5" → title + start_page
- Mehrzeilige TOC-Einträge werden zusammengeführt
- Mini-Kapitel (<150 Wörter) werden mit dem nächsten gemergt
- Strategie-Fallbacks: TOC → Heading-Pattern (4x-Decode) → Wort-Chunks
- Ergebnis TME102: 11 echte Kapitel (Einleitung, 1 Statik ebener Tragwerke, 1.2 Lager und Anschlüsse, ...)
- **WICHTIG: PDF muss neu hochgeladen werden** damit neue Kapitel greifen

### Aktueller Stand
- Git: `8896e90` auf `main`
- Vercel deployed automatisch
- PDF-Re-Upload nötig für neue Kapitel-Extraktion

---

## Lern-Bot — PDF.js Rewrite Phase 1 (09:30–10:30 UTC)

### Kontext: Problem das behoben werden sollte
- Überschriften wurden 4× vorgelesen (AKAD-PDFs haben Überschriften 4× wiederholt im PDF-Encoding)
- `unpdf` concateniert alle Items zu einem String → "1111" und "Statik ebener TragwerkeStatik ebener Tragwerke..." als ein langer String
- Bisherige Fix-Strategie: Regex-basiertes Decoding (`decodeRepeated`, `isRepeatedLine`)
- Viele Fixes (v0.8.0–v0.8.4), aber immer noch fragil

### Entscheidung: Vollständiger Rewrite auf `pdfjs-dist` direkt

**Erkenntnis aus Analyse:**
- `pdfjs-dist` ist bereits installiert (v5.5.207), `unpdf` nutzt es intern nur ohne Positionsdaten
- PDF.js `getTextContent()` liefert pro Item: `transform[4]=x, transform[5]=y, height, str`
- AKAD-Encoding im PDF: "Statik ebener Tragwerke" erscheint als **4 separate Text-Items** an (fast) gleicher y-Position — NICHT als ein langer String!
- `unpdf` concateniert diese Items → "Statik ebener TragwerkeStatik ebener Tragwerke..." (langer String)
- Mit PDF.js direkt: Gruppierung nach y-Position → 4 identische Items = Encoding erkannt, dedupliziert

**Technischer Ansatz:**
- Header-/Footer-Zone: positionsbasiert (y > 92% = Header, y < 8% = Footer) → KEIN Regex mehr
- Encoding-Erkennung: N≥2 identische Items in einer Zeile → `isEncoded=true`, `deduped=items[0].str`
- Kapitel-Erkennung: encoded NUM ("1","1","1","1") gefolgt von encoded Titel ("Statik...") → Heading
- y-Gap-Check: pendingNumber und Titel müssen < 50pt auseinander liegen (verhindert Diagramm-Label-False-Positives wie "2 Teilsystem")

### Worker-Setup für Node.js / Vercel
```typescript
import { getDocument, GlobalWorkerOptions } from 'pdfjs-dist/legacy/build/pdf.mjs';
const workerPath = resolve(process.cwd(), 'node_modules/pdfjs-dist/legacy/build/pdf.worker.mjs');
GlobalWorkerOptions.workerSrc = workerPath;
```

### Test-Ergebnis (lokal, TME102 74 Seiten)
Kapitel erkannt: 20 (vorher 11 mit TOC-Strategie, vorher ~6 falsch)
```
1. Einleitung
2. Einleitung und Lernziele
3. 1.1 Tragelemente ebener Systeme
4. 1.1.1 Stabförmige Tragelemente
...
18. Zusammenfassung
19. Antworten zu den Kontrollfragen
20. Literaturverzeichnis
```

### Gefundene Issues beim Debuggen
1. **"1 Statik ebener Tragwerke" fehlt** — y=756 "Statik ebener Tragwerke" hat n=4, isEncoded=Y, aber `chRe.test()` schlägt fehl. Noch in Analyse (stand: 10:30 UTC, session unterbrochen)
2. **False Positive "2 Teilsystem"** (p44) — Fix: y-Gap < 50pt Bedingung behebt das
3. **"1.4.2 Tragverhalten..." Titel abgeschnitten** — Überschrift zu lang, zweite Zeile "Tragsystemen" nicht erkannt

### Aktueller Code-Stand
- `route.ts` komplett neu geschrieben (pdfjs-dist, positionsbasiert)
- Version noch NICHT erhöht (Rewrite nicht committed, da Bug "1 Statik ebener Tragwerke" noch offen)
- Nächster Schritt: Debug warum p6 y=756 "Statik ebener Tragwerke" (n=4, isEncoded=Y) nicht als Heading erkannt wird

### Kritische Dateipfade
- Route: `/data/.openclaw/workspace/lernbot/app/api/documents/upload/route.ts`
- Test-PDF: `uploads/5yy1cyxd9as-TME102_11287_K1113_OC.pdf` (74 Seiten)
- Repo: `git@github.com:Vic3d/Lern-Bot.git`, SSH: `/data/.ssh/id_ed25519_vic3d`
- Live: https://lern-bot.vercel.app (aktuell noch v0.8.4 auf Vercel)

---

## Trading Monitor v2 — Großer Umbau (16:00-16:45)

### Was gemacht wurde
- **`scripts/trading_monitor.py`** geschrieben (21.944 Bytes) — ein einziges Python-Script für ALLE Kurs-Checks
  - Stops, Watchlist-Entries, Trailing-Stop-Logik, WTI/VIX Macro, Candlestick-Patterns
  - Nutzt Yahoo Finance (`query2.finance.yahoo.com`) + Onvista (scraping)
  - Schreibt Alerts in `memory/alert-queue.json` → Cron-Agent liest + sendet via message-Tool
- **`trading_config.json`** angelegt (2.880 Bytes) — alle Positionen, Stops, Watchlist zentral
- **Neuer Cron-Job:** `511278b8-3c6c-4aad-b8aa-297e7ac13a1f` — "Trading Monitor v2", alle 15 Min Mo-Fr 08-22h, Haiku, 90s Timeout
- **21 alte Cron-Jobs gelöscht** — Stop-Loss Monitor, Intraday Alert, AG Umkehrkerze 5min, diverse Einzelaktien-Monitore, etc.

### Ergebnis
- **Vorher:** ~634 Runs/Tag, ~1,6M Tokens/Tag, 31 aktive Crons
- **Nachher:** ~60 Runs/Tag, ~50k Tokens/Tag, ~12 aktive Crons
- **Erster Testlauf** hat 5 echte Alerts gefunden (EQNR trailing, MSFT/BAYN stop-nah, AG entry zone, RIO.L fehlender Stop)

### Verbleibende aktive Crons
- Morgen-Briefing 08:00, Xetra 10:00, Abend-Report 22:00 (Haiku), Tagesabschluss 23:00
- NewsWire 4x/Tag, Transcript Analyzer 4x/Tag
- Trading Monitor v2 alle 15 Min
- Wöchentliche Review So 20:00
- E-Mail Monitor (2 Jobs)

### Offen
- [ ] Erster echter Cron-Lauf Montag 08:00 — validieren
- [ ] `openclaw message send` Zuverlässigkeit für alert-queue prüfen

---

## Nachrichten-Analyse + Strategie-Check (16:45-17:00)

### Geopolitik Iran — Eskalation fortgesetzt
- Israel expandiert Strikes auf Westiran (Hamedan, Borujerd) — IRGC-HQ, Polizei, Infrastruktur
- Iranischer Marine-Kommandeur getötet in Bandar Abbas
- UAE unter Beschuss: 4 ballistische Raketen + 6 Drohnen abgefangen, 6 Tote
- Drohnenangriff auf italienisch-amerikanische Basis in Kuwait
- Trump droht mit Kharg Island Strikes (Irans Haupt-Ölexport-Terminal)
- Trump: "Taking over Strait of Hormuz" — internationale Koalition
- Iran-FM Araghchi: "Wir sind stark genug, sehen keinen Grund mit Amerikanern zu reden"
- IEA: Notreserven fließen — aber unzureichend
- EU-Außenminister diskutieren Aspidis-Marine-Mission für Hormuz

### Portfolio-Kurse (15:35 UTC)
- NVDA 157,80€ (–6,0%), MSFT 346,28€ (–1,6%), PLTR 132,15€ (±0%)
- EQNR 30,39€ (+12,4%), BAYN.DE 38,63€ (–3,3%), RIO.L 77,21€ (+0,4%)
- WTI $98,71, VIX 27,19, EUR/USD 1,1423

### Strategie-Statusänderungen
- S1 Iran/Öl: 🟢 STARK bestätigt (stärker als letzte Woche)
- S4 Silber/Gold: 🟡→🟢 UPGRADE (Gold/Silber scharf hoch, AG $22,56 in Entry B Zone)
- Rest stabil (S2 🟢, S3 🟡, S5 🟡, S6 🟢, S7 🟡)

### EQNR Stop korrigiert
- Victor wies darauf hin: Stop war schon auf 27€ nachgezogen (12.03.), nicht mehr 25€
- **Empfehlung: Stop auf 28,50€** (Dirk-Regel: >10% → 50% Gewinn sichern)
- `trading_config.json` + `projekt-trading.md` auf 28,50€ aktualisiert
- Ziel 1 (29,50€) als ✅ ERREICHT markiert, Ziel 2 (31-32€) aktiv
- **Victor muss Stop in Trade Republic manuell auf 28,50€ setzen** (Montag vor 09:00)

### Kritische Montag-Aktionen
1. 🔴 EQNR Stop 28,50€ in TR setzen
2. 🔴 BAYN.DE beobachten 09:00 (1,7% über Stop 38€)
3. 🟡 MSFT entscheiden (2,4% über Stop 338€ — halten oder senken?)
4. 🟡 NVDA Stufe 1 ($180 Zone aktiv, kein Blindkauf wegen VIX + Chip-Export-Risiko)
5. 🟢 ASML im Auge (1.178€, nur 3€ über Entry-Zone 1.175€)

---

## Lern-Bot + NextJob Fusion — Entscheidung (17:04-17:10)

### Victor: "Lernbot und NextJob sollen aufeinander aufbauen"
- **Entscheidung:** Nicht zwei getrennte Produkte, sondern EINE Plattform von Anfang an
- Lern-Bot bekommt Backend (PostgreSQL + Auth + Event-Logging)
- Jede Quiz-Antwort / Lerninteraktion = Datenpunkt für Diagnostik-Engine
- Diagnostik → Job-Matching wächst organisch aus dem Lernverhalten

### Geplanter Phasenplan
- **Phase 1a (2-3 Wo):** localStorage → PostgreSQL (Supabase Free), Auth (Magic Link/OAuth), Event-Tabelle
- **Phase 1b (2 Wo):** Quiz-Modul (Claude API generiert MC-Fragen, gecacht), Spaced Repetition
- **Phase 2 (Monat 2):** Diagnostik-Dashboard — Stärken-Radar aus Lerndaten, Job-Risiko-Check
- **Phase 3 (Monat 3-4):** Job-Matching — Job-DB (5.000+ Jobs mit AI-Resistance-Scores)

### Architektur-Entscheidung
- Ein Repo (`lernbot/`), ein Deploy (Vercel), eine URL
- Shared: Auth, User-Profil, Event-System, PostgreSQL
- Kosten: ~$5/Monat (Supabase Free + Claude API für Quiz-Gen)
- DB-Kern: `users`, `documents`, `events` (das Gold), `profile_scores`, `jobs`, `job_matches`

### Status: Warten auf Victors Go für Phase 1a Start
